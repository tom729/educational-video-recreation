---
name: educational-video-recreation
description: "根据科学或工程选题制作有配音的日常科普视频，或从参考链接、上传视频复刻教育动画；交付可编辑预览、首帧封面和竖屏视频。用于科普制作、教学动画重制，以及构图留白、自然转场、配音尾音、节奏和术语的迭代；单纯课程转录或摘要不使用本技能。"
---

# 教育动画与日常科普制作

把科学原理或参考视频的教学机制制作成可解释、可编辑、可验证的动画。有参考时重建图形关系和动作逻辑，不把原视频直接重新导出作为复刻成果；只有选题时，先核对资料再组织因果解释。

## 先选择制作模式

- **跨 Agent 首次使用或完整制作**：读取 [可移植制作流程](references/production-workflow.md)；工具和安装适配见 [跨 Agent 使用说明](references/portability.md)。不要求读取历史对话，不绑定本机路径、语音服务、浏览器或视频框架。
- **日常科学／工程科普、有解说的分段视频**：制作或修改前读取 [日常科普制作规则](references/daily-science-production.md)，按其中的构图、独立开场与拆片、术语与发音、配音尾音、转场、收尾与默认暂停要求执行。此模式默认中文配音、顶部 20%／底部和左右各 10% 留白。
- **几何解题／动态锁屏复刻**：沿用下表的几何动画要求，默认无声、顶部约 1/3 留白；具体案例按需读取文末链接。
- 用户对当前任务的新要求优先。不要把几何短片的 15–18 秒、无声和顶部 1/3 套到日常科普，也不要把某条科普的最终时长设为全系列标准。

## 默认制作预设

这些预设来自实际制作迭代，可在其他 Agent 中直接复用；用户新的明确要求优先。横屏课程、人物讲解等不同用途按实际需求调整，不强套竖屏构图。默认不在原创日常科普的画面、封面中露出品牌；来源文档可注明出处。

| 项目 | 执行标准 |
|---|---|
| 首帧即封面 | **第 0 帧直接完整出题**：题目图形、必要条件/尺寸、简短问题均可见。不能先黑屏、逐笔绘图或渐显，也不能只设置播放器 poster 来代替真实首帧。 |
| 留白 | 几何动态锁屏：顶部约 **1/3 全程留空**。日常科普：顶部 **20%**、底部及左右各 **10%**，内容集中中部。留白内不放标题、品牌、字幕；检查旋转、缩放时的运动范围也不侵入。 |
| 精确水平居中 | 按主体本身计算中心，例如正方形 `x=(画面宽度−边长)/2`。文字另行排布，不以含侧边标注的总包围盒代替主体中心。以坐标及导出画面核实，不能只目测。 |
| 文字简短、用后消失 | 几何题删除重复常驻标题，关键步骤一次只给一个短提示，如“旋转 90°”“沿对角线翻折”，通常约 1–2 秒后淡出；尺寸／符号在推理需要时保留，用后可淡出。日常科普允许保留简短主题、章节提示和配音字幕，避免一屏堆积解释。 |
| 节奏紧凑 | 简单题首帧通常读题 2–3 秒后开始解题；关键动作保持看得清的速度，步骤间短暂停顿。优先删空等待，不能只整片倍速。简单几何片常可做成约 15–18 秒，**不把本案例 16.5 秒强制套给所有题**。 |
| 数学与教学正确 | 验证题设、变换轴、关键关系和答案；同一图块要能跟踪。不能把任意变形、交叠、消失伪装成面积守恒或正确证明。 |
| 输出 | 默认 1080×1920、30 fps、H.264、yuv420p；几何解题默认无声，日常科普默认中文配音与字幕。另交付首帧 PNG、可播放预览、可编辑源码。 |

## 工作流程

### 1. 读懂参考，再决定如何重建

- 有参考时实际读取原帖或用户文件；记录标题、来源、时长、尺寸、音轨和关键视觉信息。至少检查首帧、关键变换中间帧、拼合/揭晓、结尾。原创选题则核对科学和工程资料，不虚构参考视频。
- 优先利用公开页面提供的媒体地址。短链接的 HEAD 失败不等于内容不可用，可尝试一次正常 GET 跳转。遇到权限、登录或不可访问内容，不绕过限制，不凭封面编造视频过程；确实拿不到视频再请用户提供文件。
- 区分观察与推断：原帖互动数不等于完播率；网页音频分类不等于实际听过配乐；不能仅凭画面声称原作者使用了某个软件。
- 提炼“题设 → 操作 → 可见变化 → 结论”。用户只要分析时就交付分析；要求复刻/修改时完成预览、导出和核验。先做预览是制作顺序，不自动形成审批停点；用户明确要求先审预览时遵从。

### 2. 建立正确、可复用的场景

- 二维几何和数学文字优先 SVG/Canvas；选择能精确控制所需动作的渲染方式。真实三维变化需要时再用三维工具。真人实拍型教育片先说明素材和剪辑需求，不声称二维代码能直接复刻所有实拍内容。
- 把对象、尺寸、旋转中心/翻折轴、颜色、短提示、时间轴集中为参数。预览与视频复用同一场景/时间函数；涉及随机性时固定种子。
- 第 0 帧直接显示完整题目，随后从这个状态进入解题。删掉原有开场绘图时，**同时删除它在时间轴上占用的等待**，不能只改首帧画面。
- 按数学变换生成每一块的轨迹；核实起点、终点、相邻关系、最终覆盖及结论。翻折时屏幕投影面积可缩小，不要把它误判为实际图块面积丢失。
- 改位置时所有图形、尺寸标注、辅助线共用坐标；改节奏时联动更新提示、辅助点、清理、结尾、预览跳转点和导出总帧数。

### 3. 预览与定点调整

- 网页预览至少支持播放/暂停、回到开头、倍速和进度拖动；步骤跳转与循环可选，循环默认关闭。日常科普按参考规则保持初始加载、切换和定位后暂停，不自动播放或连播。控制面板不进入视频。
- 先看真正的 `t=0`，再看变换中段和结尾。尤其检查首帧题目是否完整、主体左右边距、时钟区留白、中文字体、边缘裁切、文字淡出。
- 用户说“紧凑一点”并指出停顿时，优先改时间轴；不要误改图形尺寸。逐段区分读题、观察、结论停留与无教学作用的空停顿。
- 对声称静止过久的片段检查帧变化或确定性场景输出。中途持续约 3 秒以上的无变化片段需要检查理由；读题、思考题或答案阅读可以有明确用途的停留，不机械删掉所有静帧。
- 收到修改要求直接完成可逆修改，保留重要旧版，输出带版本号的新视频，预览和下载入口指向当前版。

### 4. 导出、核验、交付

- 逐帧渲染纯画面；不要把浏览器控制栏、原作者账号/水印、平台 UI 或原片关注片尾带入成品。
- 默认在 `outputs/<项目名>/` 保存 `index.html`、场景源码、渲染脚本、带版本号的 MP4、`cover.png`、关键帧图和简短分析说明。不要为使用本技能写死某台机器的工作目录或工具路径。
- 用本技能的 [scripts/verify_video.py](scripts/verify_video.py) 核对分辨率、帧率、实际解码帧数、时长、编码与像素格式，并从 **成品 MP4 的首帧** 提取封面。`--scan-holds` 给出疑似长停顿，需结合教学目的判断，不能当作纯技术失败。
- 日常有声科普使用 `--require-audio`，无声几何使用 `--silent`。存在音轨不等于配音正确；尾音、读音和听感仍需分别核对。
- 必须人工/视觉检查成品首帧、至少一个变换中间帧和最后结果，脚本不能代替数学正确性、居中、留白或可读性检查。
- 有配音时按 [配音与尾音规则](references/daily-science-production.md#配音与尾音) 核对各段原音、停顿及成片音轨。调整音频后同步字幕、镜头时序、章节跳转和显示时长。
- 提供当前视频、首帧封面、交互预览的可用路径；简要说明改动、时长和音频状态。不能把未导出、未检查的文件说成完成。

验证命令示例（按实际文件与时长替换）：

```bash
python3 /path/to/educational-video-recreation/scripts/verify_video.py \
  outputs/lesson/lesson-v1.mp4 --duration 16.5 \
  --cover outputs/lesson/cover.png --silent --scan-holds
```

## 何时读取案例

复刻几何拼合题、设计锁屏构图、或处理“首帧出题后停顿变长”“看起来偏左”等反馈时，读取 [references/geometry-case.md](references/geometry-case.md)。其中坐标和秒数是本次已验证的起点，不是所有教育视频的固定模板。
